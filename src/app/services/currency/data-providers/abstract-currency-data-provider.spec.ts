import { AbstractCurrencyDataProvider } from './abstract-currency-data-provider';

describe('AbstractCurrencyDataProvider', () => {
  it('should create an instance', () => {
    expect(new AbstractCurrencyDataProvider()).toBeTruthy();
  });
});
